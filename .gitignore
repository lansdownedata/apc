# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# MCP (server config with local endpoints/keys — session-local, never commit)
.mcp.json

# Virtual environment
.venv/
venv/
docs/

# Django
*.log
db.sqlite3
db.sqlite3-journal
/staticfiles/
media/*
!media/.gitkeep
!media/vehicle-types/
media/vehicle-types/*
!media/vehicle-types/.gitkeep

# Environment / secrets
.env
.env.*
!.env.example

# Node / CSS build
# NOTE: static/css/tailwind.css IS committed on purpose — Heroku runs the Python
# buildpack only, so the compiled CSS must be in the repo. Rebuild before committing
# template changes: `npm run build:css` (or keep `npm run watch:css` running in dev).
node_modules/

# OS / editors
.DS_Store
.idea/
.vscode/

# Local Claude context (personal; committed conventions live in CLAUDE.md)
CLAUDE.local.md

# SDD / agent scratch (session-local, never commit)
.superpowers/
.claude/
