# All Pro Charter — Lead Manager · base requirements
# Shared by every environment. Pin to compatible-release ranges; lock for deploys.

# --- Framework ---
Django>=5.2,<5.3              # LTS, security-supported into 2028
djangorestframework>=3.15,<3.17

# --- Config ---
django-environ>=0.11,<0.13

# --- Integrations ---
requests>=2.32,<3.0          # Zapier / LimoAnywhere / Podium HTTP
stripe>=10.0                 # deposits + off-session balance charges
cryptography>=43,<46         # encrypt LA customer passwords at rest
django-anymail[postmark]>=12,<16  # Postmark email backend (quotes + confirmations)

# --- Media ---
Pillow>=11.0,<13.0           # required by ImageField (vehicle-type photos)
phonenumbers>=8.13,<10       # E.164 normalization for contact dedupe
