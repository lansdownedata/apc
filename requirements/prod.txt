# Production — pip install -r requirements/prod.txt
-r base.txt

psycopg[binary]>=3.2         # PostgreSQL driver (Heroku Postgres); binary = no build-time libpq
gunicorn>=22.0
whitenoise>=6.7             # static file serving
sentry-sdk>=2.0            # error monitoring
django-storages[s3]>=1.14   # S3 media storage (Heroku's filesystem is ephemeral)
